import 'package:flutter/material.dart';

class DeleteMemberScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Delete Member')),
      body: Center(
        child: Text('Feature not implemented yet'),
      ),
    );
  }
}
